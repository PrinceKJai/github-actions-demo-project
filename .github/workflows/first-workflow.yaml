name: first workflow
on: [push]

jobs:
  run_shell_commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World!"
      - name: Run multiline command
        run : |
          node -v
          npm -v